def security_score(
    ram,
    defender=True,
    firewall=True
):

    score = 100

    if ram > 90:
        score -= 20

    elif ram > 80:
        score -= 10

    if not defender:
        score -= 30

    if not firewall:
        score -= 20

    return max(score, 0)